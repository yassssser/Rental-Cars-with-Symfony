
{% for flash_msg in app.session.flashBag.all %}
    {% for flash in flash_msg %}
        <div class="col-md-6 mx-auto">
                <div class="alert alert-dismissible alert-info">
                        {{ flash }}
                </div>
        </div>
        
    {% endfor %}
{% endfor %}